source 'https://rubygems.org'
ruby RUBY_VERSION

# github pages does not support 4.X jekyll version
gem 'jekyll','3.9.3'
gem 'minima'
gem 'github-pages', group: :jekyll_plugins
gem 'webrick'

group :jekyll_plugins do
  gem 'jekyll-feed'
end
